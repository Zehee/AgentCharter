# TODO_007: TPM 身份与角色名——链路表、文件名、换人过渡

> **状态**: ⏳ 讨论中，暂不执行
> **来源**: 三方讨论（Zehee + DSpro + Kimi）——2026-06-11
> **触发条件**: Zehee 想清楚后决策

---

## 问题

TPM 在链路表中写 `TPM → KIMI` 还是 `DSpro → KIMI`？

| 方案 | 链路表 | 文件名后缀 | 换人过渡 |
|------|--------|-----------|---------|
| **A. 写角色名** | `TPM → KIMI` | `@TPM` | ✅ 零成本——新人直接继承 TPM 这个名字 |
| **B. 写真名** | `DSpro → KIMI` | `@DSpro` | ❌ 换人后 @DSpro 文件无人处理 |

方案 A 清晰但维度复杂——TPM 既是角色名又是文件名的一部分，影响 `is_tpm`、`validate_agent`、`patrol` 等多个模块。

**当前暂缓决策，先避免问题：** TPM 在链路表中写真名（方案 B），不写 `TPM`。这样 `@TPM` 不会出现在文件名中，通配问题不存在。

---

## 关联的未决问题

- TPM 在 README 👑 区的声明方式（个人名 vs 角色名）
- 换人过渡期的文件处理策略
- `is_tpm` 是否要支持个人名→角色名的映射
